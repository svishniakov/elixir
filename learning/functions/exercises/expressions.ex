# 1
# sarah = fn(bread, milk, cake) -> (bread * 0.10) + (milk * 2) + (cake * 15) end

# 2
# distance = 200
# time = 4
# IO.puts("Bob has traveled #{distance} for #{time} with speed #{distance/time}")

# 3
# apply_tax = fn price -> IO.puts "Price: #{Float.round(price * 1.12, 4)} - Tax: #{Float.round(Float.round(price * 1.12, 4) - price, 4)}" end
